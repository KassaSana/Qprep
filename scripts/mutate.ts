/**
 * STUB: question mutation pipeline.
 *
 * Idea: take a base problem from `content/researcher-seed.ts`, ask Claude to
 * produce N variants with different numbers / framings while preserving the
 * underlying mechanic, then write them as new SeedQuestion entries (with new
 * slugs and recomputed answers) to a new file for review before seeding.
 *
 * Out of scope for v1; left here so the workflow has a placeholder to extend.
 */

async function main() {
  console.log("scripts/mutate.ts is a v1 stub. Wire up Claude here later.");
}

main();
