import type { SeedQuestion } from "@/content/question-types";

/**
 * System Design — empty for v1. Reserved as the topic the seed glob will
 * pick up automatically once we add full distributed-system questions.
 */
export const SYSTEM_DESIGN_SEED: SeedQuestion[] = [];
